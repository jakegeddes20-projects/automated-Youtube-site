"use client";

// Temporary shell while the queue API is being wired up; replaced by the
// full dashboard in the next step.
export default function Dashboard() {
  return <p className="subtitle">Rebuilding the dashboard for the new queue — back shortly.</p>;
}
